package com.dino.sufara.core.designsystem

import androidx.compose.ui.graphics.Color

// Glavna paleta
val BlueDeep = Color(0xFF114B5F)     // Pozadina (SpaceDark)
val BlueTeal = Color(0xFF028090)     // Kartice (MysticTeal)
val WhiteMint = Color(0xFFE4FDE1)    // Tekst uputstva (StarlightWhite)
val OrangeTerra = Color(0xFFE58F65)  // Glavna dugmad (Terracotta)
val RedCoral = Color(0xFFF45B69)     // Greške (CoralRed)

// Pomoćne boje
val WhitePure = Color(0xFFFFFFFF)    // Arapski tekst
val TealGhost = Color(0x33028090)    // Providni efekti na klik